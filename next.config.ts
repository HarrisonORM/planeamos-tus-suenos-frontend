import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Dominios externos de imágenes permitidos
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "res.cloudinary.com",
      },
      {
        protocol: "https",
        hostname: "images.unsplash.com",
      },
    ],
  },
};

export default nextConfig;